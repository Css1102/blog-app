import React from 'react'
// This is used to create a contextAPI repository in react which will contain all the props used in the
// components. It is currently empty.
const UserContext=React.createContext()

export default UserContext;